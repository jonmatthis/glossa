---
sidebar_position: 1
title: Introduction
---

import { AiGeneratedBanner, Tip } from '@freemocap/skellydocs';

<AiGeneratedBanner />

# Welcome to glossa

This documentation site is built with [Docusaurus](https://docusaurus.io/) and the <Tip shortInfo="A shared Docusaurus theme package for FreeMoCap documentation sites." longInfo="@freemocap/skellydocs provides pre-built components (IndexPage, RoadmapPage, Tip, AiGeneratedBanner), CSS design tokens, and a CLI scaffolder — so every FreeMoCap sub-project gets the same dark-theme design without duplicating code." href="https://github.com/freemocap/skellydocs">@freemocap/skellydocs</Tip> theme.

## Start here

- [Overview](./overview) — what Glossa is, the two surfaces, the assist dial, the agent architecture in one paragraph.
- [Architecture](./architecture) — components, IPC surface, the guided-turn pipeline, prompt composition, persistence.
- [Ontology](./ontology) — every domain entity, field-by-field: tokens, mechanics, scaffolds, TeachingPlan, Profile, Stories.
- [Status](./status) — honest inventory: what works, known issues (R1–R14), suggested order of battle.
- [Platforms & Build](./platforms) — Windows today; the concrete path to macOS/Linux/Android/iOS.

## Working on the docs

Edit the files under `docs/` — the sidebar is autogenerated and ordered by
each file's `sidebar_position` frontmatter. To preview: `npm install && npm
start` in this folder.

