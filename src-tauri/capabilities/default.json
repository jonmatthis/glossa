{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window and the popped-out observability window",
  "windows": [
    "main",
    "glossa-dev"
  ],
  "permissions": [
    "core:default",
    "log:default"
  ]
}
